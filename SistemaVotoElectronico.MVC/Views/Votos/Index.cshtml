@model IEnumerable<SistemaVotoElectronico.Modelos.Voto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EleccionId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdCandidatoSeleccionado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdListaSeleccionada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaRegistro)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EleccionId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdCandidatoSeleccionado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdListaSeleccionada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaRegistro)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
}
    </tbody>
</table>
